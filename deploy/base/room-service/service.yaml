apiVersion: v1
kind: Service
metadata:
  name: room-service
  namespace: pylon
  labels:
    app.kubernetes.io/name: room-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: pylon
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: room-service
  ports:
    - port: 9003
      targetPort: http
      protocol: TCP
      name: http